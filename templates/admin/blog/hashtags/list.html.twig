{% extends 'admin/base.html.twig' %}

{% block body %}
    <table class="table">
        <thead>
        <tr class="active">
            <th>{{'Name'|trans}}</th>
            <th>{{'Created at'|trans}}</th>
            <th>{{'Actions'|trans}}</th>
        </tr>
        </thead>

        <tbody>
        {% for hashTag in hashTags %}
            <tr>
                <td>{{ hashTag.name }}</td>
                <td>{{ hashTag.createdAt|date('m/d/y') }}</td>
                <td>
                    <ul class="list-inline">
                        <li>
                            <a href="{{ path('admin.blog.hashtag.edit', {id: hashTag.id}) }}" class="btn btn-default">{{'Edit'|trans}}</a>
                        </li>

                        <li>
                            <form method="post" action="{{ path('admin.blog.hashtag.delete', {id: hashTag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item');">
                                <input type="hidden" name="_method" value="DELETE">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hashTag.id) }}">
                                <button class="btn btn-danger">{{'Delete'|trans}}</button>
                            </form>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{#    <a href="{{ path('admin.blog.topic.create') }}" class="btn btn-success">{{'Create new'|trans}}</a>#}

{% endblock %}